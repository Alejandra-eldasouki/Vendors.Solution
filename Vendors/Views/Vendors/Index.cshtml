@using Vendors.Models;
<h1>Vendors:</h1>

@if (Model.Count == 0)
{
    <h3>No vendors added yet</h3>
}
@foreach (Vendor vendor in Model)
{
    <p><a href="@Url.Action("VendorOrders", "Home", new { id = vendor.Id })">@vendor.Name</a></p>
}
